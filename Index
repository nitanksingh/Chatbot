<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>🌸 Tanisha's Magical Chat Experience 💖</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #ffdde1, #ee9ca7);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            color: #333;
            animation: backgroundShift 20s infinite alternate;
        }

        @keyframes backgroundShift {
            0% { background: linear-gradient(135deg, #ffdde1, #ee9ca7); }
            50% { background: linear-gradient(135deg, #fbc2eb, #a6c1ee); }
            100% { background: linear-gradient(135deg, #ffdde1, #ee9ca7); }
        }

        .container {
            background: rgba(255, 255, 255, 0.98);
            padding: 20px;
            border-radius: 25px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            max-width: 95%;
            width: 100%;
            text-align: center;
            animation: fadeIn 2s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        h1 {
            color: #e91e63;
            font-size: 2em;
            margin-bottom: 20px;
            text-shadow: 1px 1px 5px rgba(0,0,0,0.2);
        }

        #chat {
            background: #f0f8ff;
            border-radius: 15px;
            padding: 20px;
            height: 300px;
            overflow-y: auto;
            margin-bottom: 20px;
            text-align: left;
            border: 2px solid #e91e63;
            box-shadow: inset 0 4px 12px rgba(0,0,0,0.15);
        }

        .chat-entry {
            margin: 10px 0;
            padding: 10px;
            border-radius: 12px;
            word-wrap: break-word;
        }

        .bot {
            color: #e91e63;
            background: #ffe4e1;
            align-self: flex-start;
            font-weight: bold;
        }

        .user {
            color: #2196f3;
            background: #e0f7fa;
            text-align: right;
            align-self: flex-end;
            font-weight: bold;
        }

        input[type="text"] {
            padding: 12px;
            width: 100%;
            border-radius: 12px;
            border: 1px solid #ccc;
            font-size: 1em;
            transition: box-shadow 0.3s;
            margin-bottom: 10px;
        }

        input[type="text"]:focus {
            box-shadow: 0 0 10px #e91e63;
            outline: none;
        }

        button {
            padding: 12px;
            width: 100%;
            border: none;
            border-radius: 12px;
            background: #e91e63;
            color: white;
            font-size: 1em;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
            transition: background 0.3s, transform 0.2s;
        }

        button:hover {
            background: #d81b60;
            transform: scale(1.05);
        }

        .heart {
            font-size: 3em;
            color: red;
            animation: beat 1s infinite;
            display: inline-block;
            margin-top: 20px;
        }

        @keyframes beat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.5); }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>🌸 Tanisha's Magical Chat Experience 💖</h1>
        <div id="chat">
            <div class="chat-entry bot">Hello Tanisha! 🌷 I'm your magical chatbot, created with love just for you. 💕 Ask me anything, or share your thoughts! 😊</div>
        </div>
        <input type="text" id="userInput" placeholder="Type your magical message here..." />
        <button onclick="sendMessage()">✨ Send ✨</button>
        <div class="heart">❤️</div>
    </div>

    <script>
        function sendMessage() {
            const input = document.getElementById('userInput');
            const message = input.value.trim();
            if (message === '') return;

            const chatDiv = document.getElementById('chat');

            const userEntry = document.createElement('div');
            userEntry.className = 'chat-entry user';
            userEntry.textContent = message;
            chatDiv.appendChild(userEntry);

            input.value = '';
            chatDiv.scrollTop = chatDiv.scrollHeight;

            setTimeout(() => {
                const botReply = document.createElement('div');
                botReply.className = 'chat-entry bot';
                botReply.textContent = getBotResponse(message);
                chatDiv.appendChild(botReply);
                chatDiv.scrollTop = chatDiv.scrollHeight;
            }, 1000);
        }

        function getBotResponse(input) {
            const lowercaseInput = input.toLowerCase();

            if (lowercaseInput.includes('how are you')) {
                return "I'm feeling magical because I'm talking to you, Tanisha! 🌟";
            } else if (lowercaseInput.includes('love')) {
                return "My love for you is as vast as the stars, infinite and beautiful. 🌌❤️";
            } else if (lowercaseInput.includes('hi') || lowercaseInput.includes('hello')) {
                return "Hello beautiful Tanisha! 🌸 Your presence makes everything shine brighter! ✨";
            } else if (lowercaseInput.includes('bye')) {
                return "Farewell for now, but my heart will always beat for you. 🌹";
            } else if (lowercaseInput.includes('thank')) {
                return "You're welcome, Tanisha! Your happiness is my ultimate goal. 🌈";
            } else {
                return "That's enchanting, Tanisha! Please tell me more... 🌷";
            }
        }
    </script>
</body>

</html>


